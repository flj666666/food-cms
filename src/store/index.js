import { createStore } from 'vuex'
import cellInfo from './modules/cellInfo'

export default createStore({
  state: {
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    cellInfo,
  }
})
